$content = @"
[project]
name = "aegisflow"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
]

[tool.vercel]
entrypoint = "backend.main:app"
"@
[System.IO.File]::WriteAllText("$PWD\pyproject.toml", $content)